<% if book_comment.errors.any? %>
  <div class="error_explanation">
    <h2 class="error_title"><%= pluralize(book_comment.errors.count, "error") %> prohibited this product from being saved:</h2>
    <ul class="error_specific">
      <% book_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>